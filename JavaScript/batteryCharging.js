let charge = 10;
while (charge < 100) { // loop will run until charge is less than 100

    console.log("Battery is charging: " + charge + "%"); // print the current charge percentage

    charge += 10; // increment charge by 10 in each iteration
}